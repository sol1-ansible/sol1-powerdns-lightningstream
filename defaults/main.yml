---
powerdns_lightningstream_cleanup_compiled_installation: true

powerdns_lightningstream_instance: "{{ ansible_hostname }}"
powerdns_lightningstream_lmdbs:
  main:
    path: /var/lib/powerdns/pdns.lmdb
    schema_tracks_changes: true
    options:
      no_subdir: true
      create: true
  shard:
    path: /var/lib/powerdns/pdns.lmdb-0
    schema_tracks_changes: true
    options:
      no_subdir: true
      create: true

# https://doc.powerdns.com/lightningstream/latest/configuration/index.html#sync-parameters
powerdns_lightningstream_sync:
  storage_poll_interval: 30s
  lmdb_poll_interval: 15s

# https://doc.powerdns.com/lightningstream/latest/configuration/index.html#s3-backend
# Update marker settings = costs less
powerdns_lightningstream_storage:
  type: s3
  options:
    access_key: ADD_YOUR_ACCESS_KEY_HERE
    secret_key: ADD_YOUR_SECRET_KEY_HERE
    region: ap-southeast-2 
    bucket: pdnsauth
    endpoint_url: http://203.0.113.0/
    create_bucket: false
    use_update_marker: True                 
    update_marker_force_list_interval: 15m  
# smaller keep interval than the default for less snapshots
  cleanup:
    enabled: true
    interval: 15m
    must_keep_interval: 4h
    remove_old_instances_interval: 168h

# powerdns_lightningstream_systemd_proxy: 
#   http: "http://proxy.example.com:8080"
#   https: "https://proxy.example.com:8080"

# powerdns_lightningstream_http:
#   address: ":8500"

# powerdns_lightningstream_log:
#    level: info        # "debug", "info", "warning", "error", "fatal"
#    format: human      # "human", "logfmt", "json"
#    timestamp: short   # "short", "disable", "full"  